releases:
  - name: authentik-db
    installed: {{ .Values.features.authentik.db }}
    namespace: auth
    chart: cnpg/cluster
    version: "0.5.0"
    values:
      - ../../values/authentik-db/values.yaml
