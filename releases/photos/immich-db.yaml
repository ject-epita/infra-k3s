releases:
  - name: immich-db
    installed: {{ .Values.features.immich.db }}
    namespace: photos
    chart: cnpg/cluster
    version: "0.5.0"
    values:
      - ../../values/immich-db/values.yaml
