releases:
  - name: affine-db
    installed: {{ .Values.features.affine.db }}
    namespace: productivity
    chart: cnpg/cluster
    version: "0.5.0"
    values:
      - ../../values/affine-db/values.yaml
